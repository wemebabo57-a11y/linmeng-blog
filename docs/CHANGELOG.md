# 版本变更记录

本文件按版本记录林梦博客的功能与数据库变更。

程序自身的版本号定义在 `src/<版本>/includes/config.php` 的 `LM_VERSION` 常量。

---

## v2.4.2

**程序内版本号** `LM_VERSION = 2.4.2`

一个基于原生 PHP + MySQL 的轻量博客系统，不依赖任何框架，自带可视化安装向导，上传代码后访问 `/setup` 三分钟即可上线。

### 内容管理

- 文章支持 Markdown / HTML 双模式，带封面、摘要、标签、分类、置顶、草稿、图集
- 评论与留言板：楼中楼回复、审核机制、敏感词过滤
- 标签云、分类归档、时间轴归档
- RSS 订阅

### 用户与社交

- 用户注册（需审核）/ 登录 / 资料编辑 / 头像上传
- 友情链接与友链自助申请（需审核）
- GitHub / Gitee / GitCode 三平台 OAuth 登录
- 相册：基于 GitHub 仓库的图床，自动转 jsDelivr CDN

### 工具与增强

- AI 文章摘要（兼容 OpenAI / 自定义接口，支持多 Provider）
- 服务状态监控（TCP/HTTP 探测、定时任务、历史日志）
- 天气挂件、一言（Hitokoto）、音乐播放器
- 工具页（蓝奏云解析等）、小游戏模块、赞助 / 捐赠页

### 安全与性能

- CSRF 令牌、XSS 过滤、SQL 预处理、上传图片重处理
- 通用限流、登录失败锁定、登录日志
- AES-256-CBC 加密存储 API Key 等敏感配置
- 静态资源长缓存、按页面分级的 CDN 缓存策略（Cloudflare 友好）
- 粒子背景、星空背景、深色模式、响应式布局

### 数据库迁移

本版本使用的迁移脚本见 [`migrations/`](migrations/)：

| 脚本 | 用途 |
|------|------|
| `oauth-columns.sql` | OAuth 登录所需的用户表字段 |
